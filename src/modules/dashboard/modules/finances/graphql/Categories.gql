# import * from './Operation.gql'

query Categories ($operation: Operation) {
    categories (operantion: $operation) {
        id
        description
        operation
        user {
            id
            name
            email
        }
    }
}
